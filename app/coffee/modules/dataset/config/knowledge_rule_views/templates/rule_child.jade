.row
  .col-xs-2
    //- TODO - this should be a data-attribute
    //- And managed in a behavior
    i.handle.fa.fa-fw.fa-bars(style='cursor:pointer;')

  .col-xs-8= targetAttr

  //- TODO - this should be a badge?
  .col-xs-2
    if type == 'definer'
      i.fa.fa-fw.fa-plus-square-o( data-toggle='tooltip', title='Definer' )

    if type == 'decorator'
      i.fa.fa-fw.fa-magic( data-toggle='tooltip', title='Decorator' )
